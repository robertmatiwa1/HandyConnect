import { createClient } from "@supabase/supabase-js";
function env(name:string){return Deno.env.get(name)?.trim()??""}
function serviceKey(){const raw=env("SUPABASE_SECRET_KEYS");if(raw){try{const p=JSON.parse(raw);if(typeof p.default==="string"&&p.default)return p.default}catch{}}return env("SUPABASE_SERVICE_ROLE_KEY")}
function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8"}})}
function interactiveText(body:string,ui:any){const short=ui?.body?String(ui.body).trim():"";return short&&short!==body?`${body}\n\n${short}`:body}
async function sendWhatsApp(to:string,bodyText:string,payload:any){const token=env("WHATSAPP_ACCESS_TOKEN"),phoneId=env("WHATSAPP_PHONE_NUMBER_ID"),version=env("WHATSAPP_GRAPH_VERSION");if(!token||!phoneId||!version)throw new Error("WhatsApp outbound secrets are incomplete");const ui=payload?.ui;const display=interactiveText(bodyText,ui);let body:any={messaging_product:"whatsapp",to};if(ui?.type==="buttons"&&Array.isArray(ui.buttons)&&ui.buttons.length){body.type="interactive";body.interactive={type:"button",body:{text:display},action:{buttons:ui.buttons.slice(0,3).map((b:any)=>({type:"reply",reply:{id:String(b.id).slice(0,256),title:String(b.title).slice(0,20)}}))}}}else if(ui?.type==="list"&&Array.isArray(ui.rows)&&ui.rows.length){body.type="interactive";body.interactive={type:"list",body:{text:display},action:{button:String(ui.button||"Choose").slice(0,20),sections:[{title:"Options",rows:ui.rows.slice(0,10).map((r:any)=>({id:String(r.id).slice(0,200),title:String(r.title).slice(0,24),description:r.description?String(r.description).slice(0,72):undefined}))}]}}}else{body.type="text";body.text={body:bodyText}}const r=await fetch(`https://graph.facebook.com/${version}/${phoneId}/messages`,{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify(body)});const text=await r.text();if(!r.ok)throw new Error(`Meta ${r.status}: ${text.slice(0,700)}`);return text}
Deno.serve(async(req)=>{if(req.method!=="POST")return json({error:"method_not_allowed"},405);const key=serviceKey(),url=env("SUPABASE_URL");if(!key||!url)return json({error:"server_configuration"},500);const s=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});const presented=req.headers.get("x-dispatch-token")??"";const auth=await s.rpc("verify_marketplace_dispatch_token",{p_token:presented});if(auth.error||auth.data!==true)return json({error:"unauthorized"},401);const claimed=await s.rpc("claim_notification_batch",{p_limit:50});if(claimed.error){console.error(claimed.error);return json({error:"claim_failed"},500)}let sent=0,failed=0;for(const n of claimed.data??[]){try{await sendWhatsApp(n.recipient_phone,n.body,n.payload??{});await s.rpc("finish_notification",{p_id:n.id,p_success:true,p_error:null});sent++}catch(e){const msg=e instanceof Error?e.message:String(e);console.error("notification failed",n.id,msg);await s.rpc("finish_notification",{p_id:n.id,p_success:false,p_error:msg});failed++}}return json({ok:true,claimed:(claimed.data??[]).length,sent,failed})});