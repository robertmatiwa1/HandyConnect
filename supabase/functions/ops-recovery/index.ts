import { createClient } from "@supabase/supabase-js";
const PASSWORD_HASH="1ecebee07403f6e3b9ee6e73f7202767f8a86856a115a072e7e9e74729e24ab3";
const enc=new TextEncoder();
function key(){const raw=Deno.env.get("SUPABASE_SECRET_KEYS");if(raw){try{const p=JSON.parse(raw);if(typeof p.default==="string"&&p.default)return p.default}catch{}}return Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??""}
async function hex(s:string){return [...new Uint8Array(await crypto.subtle.digest("SHA-256",enc.encode(s)))].map(b=>b.toString(16).padStart(2,"0")).join("")}
async function sig(v:string,k:string){const kk=await crypto.subtle.importKey("raw",enc.encode(k),{name:"HMAC",hash:"SHA-256"},false,["sign"]);return btoa(String.fromCharCode(...new Uint8Array(await crypto.subtle.sign("HMAC",kk,enc.encode(v))))).replaceAll("+","-").replaceAll("/","_").replaceAll("=","")}
async function token(k:string){const e=String(Math.floor(Date.now()/1000)+28800);return `${e}.${await sig(e,k)}`}
async function authed(req:Request,k:string){const c=req.headers.get("cookie")??"";const t=c.split(";").map(x=>x.trim()).find(x=>x.startsWith("hc_recovery="))?.slice(12);if(!t)return false;const [e,s]=t.split(".");return !!e&&!!s&&Number(e)>=Math.floor(Date.now()/1000)&&s===await sig(e,k)}
function html(body:string,status=200,extra:Record<string,string>={}){return new Response(body,{status,headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff","content-security-policy":"default-src 'self'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'",...extra}})}
function json(x:unknown,status=200){return new Response(JSON.stringify(x),{status,headers:{"content-type":"application/json","cache-control":"no-store"}})}
const LOGIN=(e="")=>`<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1"><style>body{font:14px Arial;background:#f3f7f5;display:grid;place-items:center;min-height:100vh}.c{width:min(420px,90vw);background:white;padding:30px;border-radius:16px;border:1px solid #dde8e3}.b{font-size:24px;font-weight:800;color:#116b49}.e{color:#8b3030;margin:10px 0}input,button{width:100%;padding:13px;margin-top:12px;box-sizing:border-box}button{background:#116b49;color:white;border:0;border-radius:9px;font-weight:700}</style><form class=c method=post><div class=b>HandyConnect · Recovery Ops</div>${e?`<div class=e>${e}</div>`:""}<input name=password type=password placeholder="Admin password" required autofocus><button>Sign in</button></form>`;
const PAGE=`<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><title>Recovery Ops</title><style>:root{--g:#116b49;--m:#687b73;--l:#dde8e3}*{box-sizing:border-box}body{margin:0;background:#f3f6f5;font:14px Arial;color:#13251e}.top{height:62px;background:#fff;border-bottom:1px solid var(--l);display:flex;align-items:center;justify-content:space-between;padding:0 22px}.brand{font-weight:800;color:var(--g);font-size:20px}.wrap{max-width:1300px;margin:auto;padding:22px}.head{display:flex;justify-content:space-between;align-items:end}.stats{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin:18px 0}.card,.box{background:white;border:1px solid var(--l);border-radius:14px}.card{padding:16px}.n{font-size:28px;font-weight:800}.lab,.muted{color:var(--m);font-size:12px}.box{overflow:hidden;margin-bottom:14px}.bh{padding:14px 16px;font-weight:800;border-bottom:1px solid var(--l)}table{width:100%;border-collapse:collapse}th,td{padding:11px 14px;border-bottom:1px solid #edf2ef;text-align:left}th{font-size:11px;color:var(--m);text-transform:uppercase}.pill{padding:4px 8px;border-radius:999px;background:#edf4f1;font-size:11px}.old{background:#fff3d5}.sent{background:#e3f5eb}.bad{background:#fde8e8}.empty{padding:24px;color:var(--m)}button{padding:9px 12px;border:0;border-radius:9px;background:var(--g);color:white;font-weight:700;cursor:pointer}@media(max-width:800px){.stats{grid-template-columns:repeat(2,1fr)}.wrap{padding:12px}.scroll{overflow:auto}table{min-width:850px}}</style></head><body><header class=top><div class=brand>HandyConnect · Recovery Ops</div><a href="?logout=1">Sign out</a></header><main class=wrap><div class=head><div><h1>Journey recovery</h1><div id=u class=muted>Loading…</div></div><button onclick=load()>Refresh</button></div><div id=s class=stats></div><div id=open></div><div id=history></div></main><script>const E=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const D=x=>x?new Date(x).toLocaleString():'—';const A=x=>Math.round((Date.now()-new Date(x).getTime())/60000);function box(t,h,r){return '<div class=box><div class=bh>'+E(t)+'</div>'+(r.length?'<div class=scroll><table><thead><tr>'+h.map(x=>'<th>'+E(x)+'</th>').join('')+'</tr></thead><tbody>'+r.join('')+'</tbody></table></div>':'<div class=empty>Nothing to show.</div>')+'</div>'}async function api(){const r=await fetch(location.pathname+'?api=1',{method:'POST'});if(r.status===401){location.reload();return}const x=await r.json();if(!r.ok)throw Error(x.error||'Failed');return x}async function load(){const x=await api();document.getElementById('s').innerHTML=[['Open stalls',x.stats.open],['Nudged',x.stats.nudged],['Sent',x.stats.sent],['Resumed',x.stats.resumed],['Recovery rate',x.stats.rate+'%']].map(a=>'<div class=card><div class=n>'+E(a[1])+'</div><div class=lab>'+E(a[0])+'</div></div>').join('');document.getElementById('open').innerHTML=box('Current stalls',['User','Flow','State','Age','Nudges','Delivery','Review'],x.open.map(q=>'<tr><td>'+E(q.external_user_id)+'</td><td>'+E(q.flow)+'</td><td>'+E(q.state)+'</td><td>'+A(q.stalled_since)+' min</td><td>'+E(q.reminder_count)+'</td><td><span class="pill '+E(q.delivery_status||'')+'">'+E(q.delivery_status||'not nudged')+'</span></td><td>'+(A(q.stalled_since)>1440?'<span class="pill old">manual review</span>':'—')+'</td></tr>'));document.getElementById('history').innerHTML=box('Recovery history',['User','Flow','State','Nudged','Resolved','Resolution'],x.history.map(q=>'<tr><td>'+E(q.external_user_id)+'</td><td>'+E(q.flow)+'</td><td>'+E(q.state)+'</td><td>'+E(D(q.last_reminded_at))+'</td><td>'+E(D(q.resolved_at))+'</td><td>'+E(q.resolution||'—')+'</td></tr>'));document.getElementById('u').textContent='Updated '+new Date().toLocaleTimeString()}load();setInterval(load,60000)</script></body></html>`;
Deno.serve(async req=>{const k=key(),url=Deno.env.get("SUPABASE_URL")??"";if(!k||!url)return new Response("Unavailable",{status:503});const u=new URL(req.url);if(u.searchParams.get("logout")==="1")return html(LOGIN(),200,{"set-cookie":"hc_recovery=; Path=/; Max-Age=0; HttpOnly; Secure; SameSite=Strict"});const ok=await authed(req,k);if(!ok){if(req.method==="POST"&&(req.headers.get("content-type")??"").includes("application/x-www-form-urlencoded")){const f=await req.formData();if(await hex(String(f.get("password")??""))===PASSWORD_HASH)return html(PAGE,200,{"set-cookie":`hc_recovery=${await token(k)}; Path=/; Max-Age=28800; HttpOnly; Secure; SameSite=Strict`});return html(LOGIN("Incorrect password"))}if(u.searchParams.get("api")==="1")return json({error:"unauthorized"},401);return html(LOGIN())}if(req.method==="GET")return html(PAGE);const s=createClient(url,k,{auth:{persistSession:false,autoRefreshToken:false}});const [stalls,notices]=await Promise.all([s.from("beta_stalled_journeys").select("*").order("stalled_since",{ascending:false}).limit(200),s.from("notification_outbox").select("recipient_phone,status,dedupe_key,sent_at,last_error,created_at").eq("kind","beta_recovery_nudge").order("created_at",{ascending:false}).limit(200)]);if(stalls.error||notices.error)return json({error:"recovery_query_failed"},500);const ns=notices.data??[],rows=(stalls.data??[]).map((q:any)=>{const n=ns.find((x:any)=>x.dedupe_key===`beta-recovery:${q.session_id}:${q.flow}:${q.state}`);return{...q,delivery_status:n?.status??null,delivery_sent_at:n?.sent_at??null}});const open=rows.filter((q:any)=>!q.resolved_at),history=rows.filter((q:any)=>q.resolved_at||q.reminder_count>0),nudged=rows.filter((q:any)=>q.reminder_count>0).length,sent=rows.filter((q:any)=>q.delivery_status==="sent").length,resumed=rows.filter((q:any)=>q.reminder_count>0&&q.resolved_at).length;return json({stats:{open:open.length,nudged,sent,resumed,rate:nudged?Math.round(resumed/nudged*1000)/10:0},open,history})});
